import React from 'react';
import Loadable from 'react-loadable';
import LoadingIcon from 'components/LoadingIcon';

/* export const RestaurantListing = Loadable({
    loader: () => import('containers/RestaurantListing'),
    loading() {
        return <LoadingIcon />
    }
}); */
